# Rapports d'analyse

Structure attendue:

- `latest-summary.json` : resume agregre pour le dashboard.
- `endpoints/analysis-endpoint-*.json` : un fichier par endpoint.

Les fichiers sont generes depuis `dashboard/analyse.html` (export) ou `scripts/analyse-openapi.py`.

